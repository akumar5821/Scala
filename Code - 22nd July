object  Factorial {
  def isFactorial(n: Int): Int = {
    var f = 1
    for (i <- 1 to n) {
      f = f * i
    }
    return f
  }
def main(args: Array[String]) {
  println("Input a number to find the factorial: ")
  var n = StdIn.readInt()
  println(isFactorial(n))
}
